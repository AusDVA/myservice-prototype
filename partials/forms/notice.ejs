<% if (locals.el.optionalID) { 
  var id = locals.el.optionalID;
} else {
  var id = locals.el.message.replace(/ +/g, '_').replace(/\?/g, "").toLowerCase(); 
}
%>

<% if (locals.el.optionalHide === true) { %>
<style>
  #<%=id %> {
    display: none;
  }
</style>
<% } %>

<% if (locals.el.optionalType) { 
  var myType = "uikit-page-alerts uikit-page-alerts--" + locals.el.optionalType;%>
  <div class="<%=myType%>" role="alert" id="<%=id%>">
<% } else { %>
  <div class="uikit-page-alerts uikit-page-alerts--info" role="alert" id="<%=id%>">
<%}%>
<%-locals.el.message%>
</div>

<!-- variants are
	optionalType = info (blue - default)
	optionalType = warning (orange)
	optionalType = error (red)
	optionalType = success (green)

	may need to put p tags around locals.el.message when not html
-->


