<% if (locals.body_text_incomplete) { 
  var body_text_incomplete = locals.body_text_incomplete;
}
%>

  <% if (locals.body_text_submitted) { 
  var body_text_submitted = locals.body_text_submitted;
} 
%>

    <% if (locals.button_text) { 
  var button_text = locals.button_text;
} 

%>
      <% if (locals.link) { 
  var link = locals.link;
}
%>

        <div class="widget-tile" id="widget-claim">
          <!-- Claims -->
          <span><span class="widget-number" id="text-reflect-incomplete">2</span><%- body_text_incomplete %>
          </span><br>
          <span><span class="widget-number" id="text-reflect-submitted">2</span><%- body_text_submitted %>
          </span>
          <div>
            <a><%- button_text %></a>
          </div>
        </div>
