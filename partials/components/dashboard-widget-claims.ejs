<% 
    var title, text, link, number, highlight, id;
    highlight ? highlight = highlight : highlight = false; 
%>

<div id="<%- id ? id : " " %>" class="dashboard-widget <%- highlight ? "dashboard-widget--pink" : " "%>">
    <a href="<%- link ? link : " " %>">
        <div class="dashboard-widget__top-border"></div>
        <div class="dashboard-widget__header">
            <h4 class="mys h4 dashboard-widget__title">
                Claims
            </h4>
        </div>
        <div>
            <div class="dashboard-widget__content">
                <span class="dashboard-widget__number">12</span>
                <span class="dashboard-widget__text">submitted</span>
            </div>
        </div>
    </a>
</div>