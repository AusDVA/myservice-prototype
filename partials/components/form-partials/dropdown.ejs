<%

form.id = formPartialsID(form.id, "dropdown");

%>

<!-- <select> -->
  
<% form.options.map(opt => { 

    if (typeof opt === "string") {
      opt = {
        text: opt
      }
    }

    %> <%#- JSON.stringify(opt) %> <%
    
    generateOption(opt);

  }) %>

<!-- </select> -->
