<div id="state-switcher">
  <h3>State switcher</h3>

  <%#- include(partials+"components/form-partials/radio", {
    label: "State",
    boxes: [
      "Empty list",
      "Populated list"
    ],
    id: "page-state"
  }); %>

  <h3>Other properties</h3>
  <%- include(partials+"components/form-partials/checkbox", {
    boxes: [
      "Not required"
    ],
    id: "not-required",
    customClass: "margin-below--none"
  }); %>
 

</div>

<script>

  var pageState;

  $(document).ready(function () {
    $(document).keypress(function (e) {
      switch (e.which) {
        case 96: //tilda
          $('#state-switcher').toggle();
          break;
        default:
      }
    });

    // $("#page-state-2").prop("checked", true);
    // $('input[name=page-state]').change(function () {
    //   pageState = $(this).val();
    //   stateSwitcher();
    // });

    // stateSwitcher();

  });

  $('#not-required-1').change(function() {
    updateNotRequired();  
  });

function updateNotRequired() {
    var myNotRequired = $("#not-required-1").is(":checked");
    console.log( myNotRequired )
    if ( myNotRequired ) {
      $("#not-required-notice-container").show();
    } else {
      $("#not-required-notice-container").hide();
    }
  } 


</script>