<%

var progress = JSON.parse(include("veteran-payment-progressbar")) 
progress.activeItem = 7;

%>

<%- include(templates+"header", {
  claim: "Veteran Payment", 
  heading: "Payment details",
  progress
}) %>

<div id="enter-bank-details">
  <%- include(partials+"components/form-partials/prepop/bank-details", {
      label: "Bank details",
      id: "bank-details-vp"
    }); %>

</div>

<div class="pre-pop-on" hidden>
  <hr>
  <legend class="uikit-text-input__label   form-block-header">
    Bank details</legend>
  <div class="kv-pair">
    <span class="kv-pair__key">Account Name:</span>
    <span class="kv-pair__value">Rebecca Orange</span>
  </div>
  <div class="kv-pair">
    <span class="kv-pair__key">Account number:</span>
    <span class="kv-pair__value">7123 8876 0023</span>
  </div>
  <div class="kv-pair">
    <span class="kv-pair__key">BSB:</span>
    <span class="kv-pair__value">654 - 788</span>
  </div>
  <div class="kv-pair">
    <span class="kv-pair__key">Financial Institution:</span>
    <span class="kv-pair__value">ANZ Bank</span>
  </div>
  <hr>
</div>

<div class="couple" hidden>

  <%- include(forms+"yes_no", {
    label: "Would your partner like to use the same bank details?",
    no_toggle: "#partner-bank-container"
  }) %>

  <%- include(partials+"components/form-partials/prepop/bank-details", {
    label: "Partner bank details",
    id: "partner-bank",
    modifiers: ["hidden"]
  }); %>

</div>

<h2>TFN details</h2>

<%- include(forms+"yes_no", {
  label: "Do you have a tax file number?",
  yes_toggle: "#tfn-container",
  no_toggle: "#need-tfn-container"
}) %>

<%- include(forms+"notice", {
  message: `<p>You need to apply for a Tax File Number from the <a href="https://www.ato.gov.au/"> Australian Taxation
      Office</a> and provide it to us if you wish to receive Veteran Payment.</p>`,
  id: "need-tfn",
  modifiers: ["hidden"]
}) %>

<%- include(forms+"text", {
  label: "Tax file number",
  id: "tfn",
  modifiers: ["hidden"]
}) %>

<div class="couple" hidden>

  <%- include(forms+"yes_no", {
    label: "Does your partner have a tax file number?",
    yes_toggle: "#tfn-partner-container",
    no_toggle: "#need-tfn-partner-container"
  }) %>

  <%- include(forms+"notice", {
    message: `<p>Your partner needs to apply for a tax file number from the <a href="https://www.ato.gov.au/"> Australian Taxation Office</a> to avoid their payment being taxed at a higher rate.</p>`,
    id: "need-tfn-partner",
    modifiers: ["hidden"]
  }) %>

  <%- include(forms+"text", {
    label: "Partner's tax file number",
    id: "tfn-partner",
    modifiers: ["hidden"]
  }) %>

</div>

<p id="save-next" hidden>Choose <b>save and next</b> to continue</p>

<%- include(forms+"pagination", {
    pagination: [
      {
        text: "Previous",
        link: "/auth/claim/veteran-payment/veteran-payment-05"
      },
      {
        text: "Cancel",
        link: "#open-modal"
      },
      {
        text: "Save and exit",
        link: "/auth/claim",
        modifiers: ["floated"]
      },
      {
        text: "Save and next",
        link: "/auth/claim/veteran-payment/veteran-payment-07",
        modifiers: ["floated", "primary"],
        id: "pagination-next"
      }
    ]
  }) %>


<script>
  function stateSwitcher() {
    if (localStorage.getItem("relationship-status") === "partnered") {
      $(".couple").show();
    } else {
      $(".couple").hide();
    }
    if (localStorage.getItem("bank-details-known") === "true") {
      $("#enter-bank-details").hide();
      $(".pre-pop-on").show();
    } else {
      $("#enter-bank-details").show();
      $(".pre-pop-on").hide();
    }
  }
</script>

<%- include("veteran-payment-state-switcher") %>

<%- include(templates+"footer") %>